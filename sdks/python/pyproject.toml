[project]
name = "vexil"
version = "0.1.0"
description = "Python SDK for Vexil - Kubernetes-native Feature Flags"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "Apache-2.0"}
dependencies = []

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
